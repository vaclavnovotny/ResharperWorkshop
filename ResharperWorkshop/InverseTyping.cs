namespace ResharperWorkshop;

public class InverseTyping
{
    private IRenameMe _renameMe = new RenameMe();

    public Task<string> Method() {
        //todo: call AsyncMethod on IRename object, put it into field _result (declare and initialize) and return that field
    }
}